# frozen_string_literal: true

RSpec.describe Superview do
  it "has a version number" do
    expect(Superview::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
